# Smoke Report: 2026-03-23

## Environment

- GPUs: `2x NVIDIA RTX PRO 6000 Blackwell Max-Q Workstation Edition`
- Python env: local `.venv`
- Hugging Face cache: `/pub7/hf-cache`

## Automated Verification

- `pytest`: `16 passed`

## Real-Model Qwen Smoke

Model: `Qwen/Qwen2.5-0.5B`

Common command prefix:

```bash
HF_HOME=/pub7/hf-cache ./.venv/bin/po-smoke \
  --model-id Qwen/Qwen2.5-0.5B \
  --device cuda:0 \
  --cache-dir /pub7/hf-cache \
  --trainable-scope lm_head \
  --prompts-per-step 4 \
  --group-size 4 \
  --max-new-tokens 18 \
  --temperature 1.3
```

Observed runs:

- `gspo`: reward mean `0.3125`, success rate `0.3125`, examples `16`, grad norm `11.0625`
- `rloo` step 0: reward mean `0.3125`, loss `12.14996`, grad norm `266.0`
- `rloo` step 1: reward mean `0.5`, loss `41.91766`, grad norm `928.0`
- `dapo`: reward mean `0.625`, success rate `0.625`, examples kept `8`, groups kept `2`, groups dropped `2`, grad norm `22.125`
- `cispo`: reward mean `0.3125`, success rate `0.3125`, loss `0.50625`, grad norm `11.0625`
- `maxrl`: reward mean `0.3125`, success rate `0.3125`, loss `14.73728`, grad norm `253.0`

## Moonshot Preflight

Command:

```bash
HF_HOME=/pub7/hf-cache ./.venv/bin/python - <<'PY'
from transformers import AutoConfig, AutoTokenizer
model_id = "moonshotai/Moonlight-16B-A3B"
config = AutoConfig.from_pretrained(model_id, trust_remote_code=True, cache_dir="/pub7/hf-cache")
tokenizer = AutoTokenizer.from_pretrained(model_id, trust_remote_code=True, cache_dir="/pub7/hf-cache")
print(type(config).__name__)
print(type(tokenizer).__name__)
print(getattr(config, "model_type", "unknown"))
PY
```

Observed output:

- config class: `DeepseekV3Config`
- tokenizer class: `TikTokenTokenizer`
- model type: `deepseek_v3`

## Notes

- The arithmetic smoke generator intentionally mixes addition, subtraction, multiplication, and nested arithmetic so grouped rollouts produce real reward variance.
- Cache redirection is required on this machine because `/pub3` is full.
- A full Moonshot/Kimi-family training smoke remains a good follow-on experiment after the main branch is synced.
